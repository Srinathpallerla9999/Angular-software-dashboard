import { TextManipulationPipe } from './text-manipulation.pipe';

describe('TextManipulationPipe', () => {
  it('create an instance', () => {
    const pipe = new TextManipulationPipe();
    expect(pipe).toBeTruthy();
  });
});
